import { DirectiveaDirective } from './directivea.directive';

describe('DirectiveaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveaDirective();
    expect(directive).toBeTruthy();
  });
});
